@* @model IEnumerable<StudentManagementSystem.Business.DTOs.ExamResultdto>

@{
    ViewData["Title"] = "Exam Results List";
}

<h2>Exam Results</h2>


<form asp-action="List" method="get" class="mb-3">
    <div class="row g-2">
       
        <div class="col-md-4">
            <select asp-items="ViewBag.Courses" name="courseId" class="form-select">
                <option value="">Select a Course</option>
            </select>
        </div>

        <div class="col-md-4">
            <select name="sortOrder" class="form-select">
                <option value="">Sort by Marks</option>
                <option value="asc" selected="@(ViewBag.SortOrder == "asc" ? "selected" : null)">Ascending</option>
                <option value="desc" selected="@(ViewBag.SortOrder == "desc" ? "selected" : null)">Descending</option>
            </select>
        </div>


        
        <div class="col-md-1">
            <button type="submit" class="btn btn-primary w-100">Filter</button>
        </div>
    </div>
</form>

@if (!Model.Any())
{
    <div class="alert alert-info mt-4" role="alert">
        No results available for the selected course.
    </div>
}
else
{
    <table class="table table-bordered mt-4">
        <thead>
            <tr>
                <th>Student Name</th>
                <th>Course</th>
                <th>Marks</th>
                <th>Grade</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.StudentName</td>
                    <td>@item.CourseName</td>
                    <td>@item.Marks</td>
                    <td>@item.Grade</td>
                </tr>
            }
        </tbody>
    </table>
} *@


@model IEnumerable<StudentManagementSystem.Web.Models.ExamResultDisplayViewModel>

@{
    ViewData["Title"] = "Exam Results List";
}

<h2>Exam Results</h2>

<form asp-action="List" method="get" class="mb-3">
    <div class="row g-2">
        <div class="col-md-4">
            <select asp-items="ViewBag.Courses" name="courseId" class="form-select">
                <option value="">Select a Course</option>
            </select>
        </div>
        <div class="col-md-4">
            <select name="sortOrder" class="form-select">
                <option value="">Sort by Marks</option>
                <option value="asc" selected="@(ViewBag.SortOrder == "asc" ? "selected" : null)">Ascending</option>
                <option value="desc" selected="@(ViewBag.SortOrder == "desc" ? "selected" : null)">Descending</option>
            </select>
        </div>
        <div class="col-md-1">
            <button type="submit" class="btn btn-primary w-100">Filter</button>
        </div>
    </div>
</form>

<div id="examResultsTable">
    @await Html.PartialAsync("_ExamResultsTable", Model)
</div>